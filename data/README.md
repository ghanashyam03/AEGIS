# Data directory

This directory is reserved for downloaded source data, derived data, and
run-specific artifacts. Its contents are ignored by Git, except for this file.

No data are required for the repository foundation. The planned source and
acquisition constraints are documented in `docs/decisions/001-dataset-selection.md`.
